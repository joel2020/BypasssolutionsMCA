# Bypass Solution public SPA / CRM URLs (names only; set real values in Vercel or .env.local)
VITE_APP_URL=
VITE_ADMIN_URL=

# Supabase browser credentials. The anon key is safe in the browser only when RLS is enabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
